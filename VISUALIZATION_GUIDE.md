# 📊 視覺化版本使用指南

## 🎯 新增功能總覽

升級版 Streamlit 應用 (`app_enhanced.py`) 包含 **4 個主要頁面**：

### **1. 🏠 Home - 即時預測**
- 輸入訊息即時預測 Spam/Ham
- 顯示預測信心分數
- 快速範例按鈕
- 預處理細節展示

### **2. 📊 Data Overview - 數據分析**
#### **功能：**
- **數據統計**：總訊息數、Ham/Spam 數量、比例
- **類別分佈圖**：
  - 長條圖（Bar Chart）
  - 圓餅圖（Pie Chart）
- **訊息長度分析**：
  - 整體長度分佈直方圖
  - 按類別的長度分佈對比
  - 統計表格（平均值、中位數、最大最小值）
- **詞雲圖（Word Clouds）**：
  - Ham 訊息詞雲（綠色系）
  - Spam 訊息詞雲（紅色系）

### **3. 🎯 Model Performance - 模型性能**
#### **功能：**
- **模型比較表格**：
  - 4 個模型的 Accuracy, Precision, Recall, F1-Score 對比
  - 自動高亮最佳指標
- **性能對比圖**：
  - 長條圖比較 Accuracy vs F1-Score
  - Precision vs Recall 散點圖
- **混淆矩陣（Confusion Matrix）**：
  - 熱力圖視覺化
  - True Positives, False Positives 等指標
- **ROC 曲線**：
  - 顯示模型分類能力
  - AUC 分數標註

### **4. 🔍 Feature Analysis - 特徵分析**
#### **功能：**
- **Top Tokens by Class**：
  - Top 20 Ham 常見詞
  - Top 20 Spam 常見詞
  - 對比表格
- **TF-IDF 權重**：
  - 顯示特徵總數
  - 隨機抽樣顯示特徵名稱
- **N-gram 分析**：
  - Ham Bigrams（雙詞組合）
  - Spam Bigrams（雙詞組合）

---

## 🚀 快速開始

### **步驟 1：選擇使用哪個版本**

你有 **2 個版本** 可以選擇：

#### **選項 A：基礎版（適合快速測試）**
```bash
streamlit run app.py
```
- ✅ 輕量、啟動快
- ✅ 只有預測功能
- ❌ 無視覺化分析

#### **選項 B：完整視覺化版（推薦用於 Demo）**
```bash
streamlit run app_enhanced.py
```
- ✅ 完整的 4 頁面功能
- ✅ 所有視覺化圖表
- ✅ 適合展示和報告
- ⚠️ 需要先訓練模型

---

### **步驟 2：使用增強版訓練腳本**

為了支援完整視覺化，請使用增強版訓練腳本：

```bash
python src/train_enhanced.py --data data/sms_spam_no_header.csv
```

**這個腳本會額外生成：**
- ✅ `models/metrics_comparison.csv` - 模型比較數據
- ✅ `models/confusion_matrix.npy` - 混淆矩陣
- ✅ `models/roc_curve_data.json` - ROC 曲線數據
- ✅ `models/metrics_comparison.png` - 性能對比圖
- ✅ `models/confusion_matrix.png` - 混淆矩陣圖

**訓練輸出範例：**
```
Loading data from data/sms_spam_no_header.csv
Dataset shape: (5574, 2)
...
✅ Training completed successfully!
📊 Evaluation data saved to models/
```

---

### **步驟 3：啟動增強版應用**

```bash
streamlit run app_enhanced.py
```

**首次啟動會看到：**
```
You can now view your Streamlit app in your browser.
Local URL: http://localhost:8501
Network URL: http://192.168.x.x:8501
```

---

## 📸 各頁面功能詳解

### **🏠 Page 1: Home**

**主要功能：**
1. **快速範例按鈕**：點擊載入預設的 Spam/Ham 範例
2. **文字輸入框**：輸入或貼上要分析的訊息
3. **分析按鈕**：執行預測
4. **結果顯示**：
   - 綠色卡片（Ham）或紅色卡片（Spam）
   - 信心百分比
5. **預處理細節**：展開查看文字清理過程

**使用技巧：**
- 先用範例測試功能
- 自己輸入常見的垃圾郵件關鍵字（如 "FREE", "WIN", "CALL NOW"）
- 測試正常訊息（如問候、會議邀請）

---

### **📊 Page 2: Data Overview**

**Tab 1: 類別分佈**
- **左側**：長條圖顯示 Ham vs Spam 數量
- **右側**：圓餅圖顯示比例
- **用途**：快速了解數據集是否平衡

**Tab 2: 訊息長度**
- **左圖**：所有訊息的長度分佈（直方圖）
- **右圖**：Ham vs Spam 長度對比
- **統計表**：各類別的平均、中位數、最大最小值
- **發現**：Spam 訊息通常較長（約 138 字元 vs Ham 71 字元）

**Tab 3: 詞雲**
- **綠色詞雲**：Ham 訊息中最常出現的詞
- **紅色詞雲**：Spam 訊息中最常出現的詞
- **用途**：直觀看出兩類訊息的用詞差異

---

### **🎯 Page 3: Model Performance**

**頂部表格：**
- 4 個模型的完整指標對比
- 最佳指標會高亮顯示（淺綠色）
- 可排序查看

**Tab 1: 指標比較**
- **左圖**：長條圖對比 Accuracy 和 F1-Score
- **右圖**：Precision vs Recall 散點圖
  - 越接近右上角 = 性能越好

**Tab 2: 混淆矩陣**
- **熱力圖**：顯示分類結果
  - 左上（True Negative）：正確識別的 Ham
  - 右下（True Positive）：正確識別的 Spam
  - 左下（False Negative）：漏報的 Spam ⚠️
  - 右上（False Positive）：誤報的 Ham ⚠️
- **4 個指標卡**：顯示各類別數量

**Tab 3: ROC 曲線**
- **紅線**：模型的 ROC 曲線
- **黑虛線**：隨機分類器（基準線）
- **AUC 分數**：越接近 1.0 越好（>0.99 = 優秀）

---

### **🔍 Page 4: Feature Analysis**

**Tab 1: Top Tokens**
- **左側**：Ham 訊息中最常出現的 20 個詞
  - 預期：日常用語（如 "ok", "thanks", "call", "time"）
- **右側**：Spam 訊息中最常出現的 20 個詞
  - 預期：促銷用語（如 "free", "call", "text", "claim"）
- **對比表**：並排顯示前 10 個詞

**Tab 2: TF-IDF 權重**
- 顯示向量化後的特徵總數（通常 3000 個）
- 隨機抽樣 50 個特徵展示
- 解釋 TF-IDF 的概念

**Tab 3: N-grams 分析**
- **Bigrams**：連續的兩個詞組合
- **Ham Bigrams**：正常訊息中常見的詞組
- **Spam Bigrams**：垃圾訊息中常見的詞組
  - 預期看到："free prize", "call now", "text win" 等

---

## 🎨 自訂視覺化

### **想要改變圖表顏色？**

編輯 `app_enhanced.py`，找到對應的繪圖代碼：

```python
# 範例：改變 Spam 顏色從紅色到橙色
colors = ['#2ecc71', '#ff8c00']  # 原本是 '#e74c3c' (紅色)
```

### **想要增加更多圖表？**

在對應的頁面函數中新增：

```python
def page_data_overview():
    # ... 現有代碼 ...
    
    # 新增一個 Tab
    tab1, tab2, tab3, tab4 = st.tabs([..., "📈 New Tab"])
    
    with tab4:
        st.subheader("My Custom Visualization")
        # 你的繪圖代碼
```

### **想要匯出圖表？**

訓練時已自動生成 PNG 圖檔在 `models/` 目錄：
- `metrics_comparison.png`
- `confusion_matrix.png`

直接用於報告或 PPT！

---

## 💡 Demo 展示技巧

### **建議的展示順序：**

1. **開場（Page 1 - Home）**
   - 「這是一個即時的垃圾郵件偵測系統」
   - 點擊 Spam 範例 → 展示紅色警告
   - 點擊 Ham 範例 → 展示綠色通過
   - 自己輸入一個訊息測試

2. **數據說明（Page 2 - Data Overview）**
   - 「我們使用了 5574 筆 SMS 訊息訓練」
   - 展示類別分佈圖：「13.4% 是垃圾訊息」
   - 展示詞雲：「看看垃圾訊息常用的詞」

3. **模型性能（Page 3 - Model Performance）**
   - 「我們訓練了 4 個模型，SVM 表現最好」
   - 展示比較表：「準確率達到 98.1%」
   - 展示混淆矩陣：「只有 8 個漏報」

4. **深入分析（Page 4 - Feature Analysis）**
   - 「系統是如何判斷的呢？」
   - 展示 Top Tokens：「free, win, prize 是明顯的垃圾詞」
   - 展示 Bigrams：「free prize, call now 這種組合最危險」

5. **回到首頁測試（Page 1）**
   - 「讓我們來測試一個真實的例子」
   - 輸入：「Congratulations! You won $1000! Click here!」
   - 展示高信心度的 Spam 判斷

---

## 🐛 常見問題

### **Q1: 啟動後顯示 "Model files not found"**
**A**: 先執行訓練：
```bash
python src/train_enhanced.py
```

### **Q2: 詞雲圖無法顯示中文**
**A**: 本專案針對英文，如需中文支援需額外配置字體

### **Q3: Page 3 的數據都是假的嗎？**
**A**: 如果你用 `train_enhanced.py` 訓練，數據會自動載入真實結果。否則使用範例數據。

### **Q4: 圖表太小/太大**
**A**: 調整 `figsize` 參數：
```python
fig, ax = plt.subplots(figsize=(12, 8))  # 增大圖表
```

### **Q5: Streamlit Cloud 部署失敗**
**A**: 確認 `requirements.txt` 包含所有依賴：
```txt
wordcloud>=1.9.0  # 必須加這個
nltk>=3.8
```

---

## 📦 檔案清單

### **核心檔案**
- `app.py` - 基礎版（只有預測）
- `app_enhanced.py` - **完整視覺化版（推薦）**
- `src/train.py` - 基礎訓練腳本
- `src/train_enhanced.py` - **增強版訓練（推薦）**

### **訓練後生成的檔案**
```
models/
├── spam_classifier.pkl          # 訓練好的模型
├── vectorizer.pkl               # TF-IDF 向量化器
├── model_metadata.txt           # 文字格式的元數據
├── model_metadata.json          # JSON 格式的元數據
├── metrics_comparison.csv       # 模型比較數據
├── metrics_comparison.png       # 性能對比圖
├── confusion_matrix.npy         # 混淆矩陣數組
├── confusion_matrix.png         # 混淆矩陣圖
└── roc_curve_data.json          # ROC 曲線數據
```

---

## 🎓 給老師看的重點

1. **技術實作**：
   - ✅ 使用 OpenSpec 規範開發
   - ✅ 訓練了 4 種機器學習模型
   - ✅ 達到 98%+ 準確率

2. **視覺化分析**：
   - ✅ 多維度數據探索（EDA）
   - ✅ 完整的模型評估指標
   - ✅ 特徵重要性分析

3. **工程實踐**：
   - ✅ 模組化代碼設計
   - ✅ 命令行參數支援
   - ✅ 完整的文檔和註釋

4. **實際應用**：
   - ✅ 可部署的 Web 應用
   - ✅ 即時預測功能
   - ✅ 用戶友好的介面

---

## 🚀 部署到 Streamlit Cloud

**重要：使用增強版時需要注意：**

1. **確保數據集在 repo 中**：
   - 將 `sms_spam_no_header.csv` 加入 Git
   - 或在 Streamlit Cloud 中自動下載

2. **requirements.txt 必須完整**：
```txt
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
nltk>=3.8
streamlit>=1.28.0
matplotlib>=3.7.0
seaborn>=0.12.0
wordcloud>=1.9.0
```

3. **在 `app_enhanced.py` 開頭加上自動下載數據集**：
```python
import os
if not os.path.exists('data/sms_spam_no_header.csv'):
    import urllib.request
    os.makedirs('data', exist_ok=True)
    url = 'https://raw.githubusercontent.com/PacktPublishing/...'
    urllib.request.urlretrieve(url, 'data/sms_spam_no_header.csv')
```

4. **部署設定**：
   - Main file: `app_enhanced.py`
   - Python version: 3.9+

---

## 📞 需要幫助？

如果遇到問題，檢查：
1. ✅ 是否執行了 `train_enhanced.py`
2. ✅ `models/` 目錄是否有所有檔案
3. ✅ `data/` 目錄是否有數據集
4. ✅ 所有依賴是否安裝完整

**祝你 Demo 成功！** 🎉
